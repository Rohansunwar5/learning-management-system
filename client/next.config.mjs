/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'res.cloudinary.com', 'assets.aceternity.com', 'aceternity.com', 'res.cloudinary.com'],
  }
};

export default nextConfig;
